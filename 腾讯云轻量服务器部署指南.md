# JaguarTV Content Factory 腾讯云轻量服务器部署指南

适用环境：腾讯云轻量应用服务器，推荐 Ubuntu 22.04 / Ubuntu 24.04，4 核 8G 起步。  
当前目标：把 GitHub 私有仓库同步到服务器，在服务器上运行 JaguarTV Content Factory UI 和视频处理服务。

---

## 1. 当前推荐部署结构

```text
GitHub 私有仓库
  ↓ git pull
腾讯云轻量服务器
  ├── /opt/jaguartv-content-factory-vnext
  ├── Python venv
  ├── FFmpeg
  ├── Node.js / Remotion
  ├── workspace/             # 服务器本地素材、数据库、成片，不进入 git
  └── systemd 服务
```

访问地址：

```text
http://服务器公网IP:8787/
```

> 注意：Phase 1 暂时没有完整账号权限系统。公网开放前，至少应在腾讯云安全组中限制访问 IP，或后续加 Nginx Basic Auth / Cloudflare Access。

---

## 2. 你需要准备的信息

给 Codex 或开发同事部署时，需要以下信息：

1. 服务器公网 IP；
2. SSH 用户名，通常是 `root` 或 `ubuntu`；
3. SSH 登录方式：
   - 推荐：SSH 私钥；
   - 不推荐：直接把服务器密码发到聊天里；
4. 服务器系统版本；
5. 是否允许开放 8787 端口。

---

## 3. GitHub 私有仓库访问方式

仓库地址：

```text
https://github.com/0925lyz/jaguartv-content-factory-vnext-l4
```

因为仓库是私有的，服务器必须具备 GitHub 拉取权限。推荐使用 Deploy Key。

### 3.1 推荐方式：Deploy Key

登录服务器：

```bash
ssh root@服务器公网IP
```

生成服务器专用 GitHub SSH key：

```bash
ssh-keygen -t ed25519 -C "jaguartv-server" -f ~/.ssh/jaguartv_github -N ""
```

查看公钥：

```bash
cat ~/.ssh/jaguartv_github.pub
```

到 GitHub 仓库添加：

```text
Repository → Settings → Deploy keys → Add deploy key
```

权限选择：

- 只需要服务器拉代码：不要勾选 `Allow write access`；
- 如果服务器需要自动推代码：才勾选 `Allow write access`，一般不建议。

服务器配置 SSH：

```bash
cat >> ~/.ssh/config <<'EOF'
Host github-jaguartv
  HostName github.com
  User git
  IdentityFile ~/.ssh/jaguartv_github
  IdentitiesOnly yes
EOF

chmod 600 ~/.ssh/config
ssh -T github-jaguartv
```

部署时使用这个仓库地址：

```text
git@github-jaguartv:0925lyz/jaguartv-content-factory-vnext-l4.git
```

---

## 4. 首次部署

服务器上执行：

```bash
REPO_URL=git@github-jaguartv:0925lyz/jaguartv-content-factory-vnext-l4.git \
APP_DIR=/opt/jaguartv-content-factory-vnext \
JAGUARTV_HOST=0.0.0.0 \
JAGUARTV_PORT=8787 \
bash -c "$(curl -fsSL https://raw.githubusercontent.com/0925lyz/jaguartv-content-factory-vnext-l4/main/scripts/server-install.sh)"
```

如果 raw 文件因为私有仓库无法直接访问，可以先 clone，再执行：

```bash
git clone git@github-jaguartv:0925lyz/jaguartv-content-factory-vnext-l4.git /opt/jaguartv-content-factory-vnext
cd /opt/jaguartv-content-factory-vnext
APP_DIR=/opt/jaguartv-content-factory-vnext \
JAGUARTV_HOST=0.0.0.0 \
JAGUARTV_PORT=8787 \
bash scripts/server-install.sh
```

---

## 5. 腾讯云安全组设置

在腾讯云轻量服务器控制台开放：

```text
TCP 22    SSH
TCP 8787  JaguarTV UI
```

更安全的做法：

- 22 端口只允许你的办公 IP；
- 8787 端口只允许你和同事的固定 IP；
- 如果无法固定 IP，建议后续用 Nginx + Basic Auth 或 Cloudflare Tunnel。

---

## 6. 验证服务

查看服务：

```bash
sudo systemctl status jaguartv-content-factory-vnext --no-pager
```

查看日志：

```bash
sudo journalctl -u jaguartv-content-factory-vnext -f
```

本机检查：

```bash
curl -I http://127.0.0.1:8787/
```

外网访问：

```text
http://服务器公网IP:8787/
```

---

## 7. 后续同步 GitHub 更新

以后本地开发推送到 GitHub 后，服务器执行：

```bash
cd /opt/jaguartv-content-factory-vnext
bash scripts/server-sync.sh
```

它会自动：

1. `git pull` 最新代码；
2. 更新 Python 包；
3. 重启 systemd 服务；
4. 输出服务状态。

---

## 8. 服务器上的视频存在哪里

默认都在：

```text
/opt/jaguartv-content-factory-vnext/workspace/
```

主要目录：

```text
workspace/jobs/              # 下载源视频、处理中间文件
workspace/ready_for_review/  # 成片审核包
workspace/factory.db         # 当前 SQLite 数据库
workspace/sessions/          # 登录态，不上传 GitHub
```

这些内容不会同步到 GitHub。GitHub 只放源码、配置、脚本和文档。

---

## 9. 当前阶段不建议做的事

Phase 1 不建议直接做：

- 公网裸奔长期开放；
- 多人同时大量制作同一个 SQLite 数据库；
- 自动发布；
- 把登录态、视频素材、数据库提交到 GitHub；
- 在没有访问控制的情况下暴露后台。

---

## 10. Codex 自动部署需要你提供什么

如果要让我在这个窗口里继续自动部署，请提供：

```text
服务器公网 IP：
SSH 用户名：
登录方式：本机是否已经能 ssh 登录，还是需要配置 SSH key？
系统：Ubuntu 22.04 / Ubuntu 24.04 / 其他？
是否允许开放 8787 端口：是 / 否
```

最理想状态是你先在本机确认：

```bash
ssh root@服务器公网IP
```

可以登录。只要这一步通了，我就可以继续帮你完成服务器安装、GitHub 同步和服务启动。
