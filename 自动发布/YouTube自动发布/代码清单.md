# YouTube 自动发布代码清单

## 队列与路由

`src/jaguartv_factory/publisher.py`

- 读取 `publishing` 配置。
- 找到 YouTube 账号。
- 提取候选分类、标签和审核包元数据。
- 校验来源平台是否允许发布。
- 按 `daily_limit` 和 `schedule_times` 计算发布时间。
- 创建 `publications` 队列记录。
- 为仪表盘提供 `publication_state`。

关键函数：

- `youtube_accounts`
- `resolve_publish_routes`
- `source_allowed_for_publish`
- `next_publish_slot`
- `enqueue_approved_publication`
- `auto_enqueue_approved_publication`
- `publication_state_for_candidates`

## 发布 worker

`src/jaguartv_factory/publish_worker.py`

- 扫描 `QUEUED` 和 `SCHEDULED` 的到期 YouTube 任务。
- 将任务状态置为 `PUBLISHING`。
- 调用上传器。
- 成功后回写 `PUBLISHED`、YouTube 视频 ID 和链接。
- 失败后写入 `FAILED`、`error` 和 `error_json`。

关键函数：

- `due_publications`
- `publish_due_once`
- `run_publish_worker`

## YouTube API 上传

`src/jaguartv_factory/youtube_publisher.py`

- 从环境变量和 `youtube_channel_auths` 获取 OAuth 授权。
- 解密 refresh token。
- 刷新 access token。
- 使用 YouTube resumable upload 上传 mp4。
- 返回 `youtube_video_id` 和 `youtube_url`。

关键函数：

- `youtube_access_token`
- `decrypt_refresh_token`
- `publication_video_path`
- `assert_ffprobe_readable`
- `upload_video_resumable`
- `upload_youtube_publication`

## CLI

`src/jaguartv_factory/cli.py`

新增命令：

```bash
jaguartv --config config/pipeline.yaml publish-queue --candidate <id>
jaguartv --config config/pipeline.yaml publish-queue --candidate <id> --dry-run
jaguartv --config config/pipeline.yaml publish-worker --once --dry-run
jaguartv --config config/pipeline.yaml publish-worker --once --limit 3
jaguartv --config config/pipeline.yaml publish-worker --sleep 60 --limit 3
jaguartv --config config/pipeline.yaml publish --candidate <id>
jaguartv --config config/pipeline.yaml publish-status
```

## 数据库 schema

`src/jaguartv_factory/core.py`

新增或扩展：

- `candidates.published_flag`
- `publications.package_id`
- `publications.asset_id`
- `publications.variant`
- `publications.source_platform`
- `publications.account_label`
- `publications.channel_id`
- `publications.title`
- `publications.description`
- `publications.tags_json`
- `publications.privacy_status`
- `publications.youtube_video_id`
- `publications.youtube_url`
- `publications.post_url`
- `publications.error_json`
- `publications.timezone`
- `publications.reviewer`
- `publications.review_decision_at`
- `youtube_channel_auths`

## 仪表盘

`src/jaguartv_factory/dashboard.py`

- YouTube OAuth 授权入口和回调。
- `save_review(APPROVED)` 后自动调用入队。
- 候选列表返回 `publication_state`。

`src/jaguartv_factory/web/app.js`

- 状态列显示：

```text
审核通过
已发布至 YouTube 账号：账号名 · YouTube 链接 · 视频ID
```

`src/jaguartv_factory/web/styles.css`

- `publication-note` 样式。

## 测试

`tests/test_publisher.py`

- YouTube 来源门禁。
- 非 YouTube 来源自动进入 `jaguartv_vivo` 队列。
- 无路由时阻塞。
- 重复审核不重复入队。
- 每日发布上限顺延。
- 优先选择通用版。
- 发布成功后候选行显示账号、视频 ID 和链接。

`tests/test_dashboard.py`

- 分类到账户路由。
- YouTube 来源候选禁止自动排到受限账号。
- OAuth start URL。
- OAuth callback 加密 refresh token。
