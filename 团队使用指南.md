# JaguarTV 视频工厂团队使用指南

## 1. 团队统一环境

项目只维护一份共享目录和任务数据库，所有 Agent 通过 candidate ID 交接。

```bash
cd "/Users/allen/Documents/视频二创"
export JAGUARTV_FACTORY_ROOT="/Users/allen/Documents/视频二创"
./scripts/bootstrap.sh
.venv/bin/workbuddy doctor
```

首次在新电脑安装技能：

```bash
./scripts/install-agent-skill.sh
```

## 2. WorkBuddy / CodyBuddy / Codex 调用

新建任务时明确写出技能名、工作阶段和数量或 candidate ID。

素材侦察：

```text
使用 jaguartv-content-factory 技能，从 YouTube 发现 10 条非葡语足球视频。
按分数排序，只返回 candidate ID、标题、来源链接和推荐理由，不开始制作。
```

葡语制作：

```text
使用 jaguartv-content-factory 技能，下载并制作 candidate 53e37cd26b1b965f。
必须完全移除源视频音轨，使用自然 pt-BR 配音和 Funk BGM，完成后返回审核目录。
```

成片质检：

```text
使用 jaguartv-content-factory 技能，质检 candidate 53e37cd26b1b965f。
检查原语言音轨是否消失、葡语是否清楚、BGM 是否压住人声、字幕、尺寸、品牌和 metadata；输出 APPROVED 或 REVISION_REQUIRED。
```

## 3. 无 Agent 时直接运行

```bash
# 发现素材
.venv/bin/workbuddy discover --platform youtube --limit 10
.venv/bin/workbuddy list --status DISCOVERED --limit 20

# 下载与制作
.venv/bin/workbuddy download --candidate <candidate-id>
.venv/bin/workbuddy produce --candidate <candidate-id>

# 刷新人工审核页面
.venv/bin/workbuddy review
python3 -m http.server 8765 --directory workspace/ready_for_review
```

浏览器打开 `http://127.0.0.1:8765/`。

独立的内容生产与增长控制台不依赖 WorkBuddy：

```bash
.venv/bin/workbuddy ui --host 127.0.0.1 --port 8787
```

浏览器打开 `http://127.0.0.1:8787/`。可信局域网共享时使用 `--host 0.0.0.0`，团队通过主机局域网 IP 访问。

## 4. BGM 使用规则

- 默认完全删除源视频音轨，不保留中文或其他语言人声。
- `assets/bgm/` 为空时，系统使用自动生成的原创 150 BPM Funk 节拍。
- 团队选好的热门、活动或授权曲目放入 `assets/bgm/`，支持 MP3、M4A、AAC、WAV、FLAC。
- 每个 candidate ID 会稳定选择一首曲目，多次重做不会随机变化。
- 指定单曲时，在 `config/pipeline.yaml` 设置 `audio.bgm_path`。
- 调整背景音量时修改 `audio.bgm_volume`，当前默认 `0.42`，并由侧链压缩在人声出现时自动降低。

## 5. 中文字幕处理

- 烧录在视频画面里的中文字幕无法像字幕轨一样关闭。
- 默认会裁掉源前景画面底部 18%，去除常见的底部中文字幕，再将主体重新居中。
- 如果某类来源字幕位置更高，调整 `config/pipeline.yaml` 的 `source_subtitle_crop_bottom_ratio`。
- 没有烧录字幕的来源可将 `source_subtitle_cleanup` 改为 `off`。
- 当前快速方案采用可控裁切，不做速度慢且容易产生画面瑕疵的逐帧 AI 修复。

## 6. 多人协作边界

| 角色 | 输入 | 输出 |
|---|---|---|
| 素材侦察 | 关键词、平台、数量 | `DISCOVERED` candidate ID |
| 葡语本地化编辑 | `DOWNLOADED` candidate ID | `ready_for_review/<id>/` |
| 成片质检 | 审核目录 | `APPROVED` 或修改清单 |
| 内容交付负责人 | 各角色回传 | 审核队列与最终交付状态 |

禁止两个成员同时制作同一个 candidate ID。所有人只通过共享 `workspace/factory.db` 和 `workspace/ready_for_review/<id>/` 交接，不复制第二套数据库。

## 7. 验收文件

每条成片目录至少包含：

- `video.mp4`：葡语成片。
- `cover.jpg`：封面抽帧。
- `metadata.json`：来源、葡语文案、BGM 和音频策略。
- `review.json`：人工审核结果。

任务中间文件位于 `workspace/jobs/<id>/`，包括脚本、字幕、配音、QA 和 manifest。
