# JaguarTV 内容工厂完整使用与营销增长方案

**版本：** v1.0  
**日期：** 2026-07-20  
**适用范围：** JaguarTV Hoje 消费者内容矩阵、Facebook、YouTube、TikTok、Kwai，以及后续 Instagram/WhatsApp 承接  
**系统入口：** `http://127.0.0.1:8787/`

## 1. 执行结论

JaguarTV 内容工厂的定位不是“批量搬运视频”，而是一个以巴西用户为目标、以 JaguarTV 下载、注册和首次观看为结果的内容供应链。

当前版本已经能够完成：

1. 发现并保存非葡语候选视频。
2. 下载指定视频并生成任务 ID。
3. 移除源音轨，生成 pt-BR 旁白与字幕。
4. 加入 Funk 风格 BGM、JaguarTV 角标和结尾卡。
5. 生成 1080×1920 审核成片、封面、文案和任务清单。
6. 在本地 UI 中查看库存、启动任务、安排发布队列、录入平台表现和观察转化漏斗。
7. 让 WorkBuddy、CodyBuddy、Codex 或团队成员通过同一 skill 和 candidate ID 协作。

当前版本不能完成：

1. 无人值守地稳定抓取全部中外平台。
2. 自动识别所有视频的精彩片段并生成真正的平台差异版本。
3. 自动发布到真实平台账号。
4. 自动拉取平台播放、互动和 JaguarTV 下载注册数据。
5. 直接让两台电脑安全并发处理同一共享 SQLite 数据库。
6. 自动训练模型并自主修改生产策略。

因此，当前系统适合“单机生产与业务验证”；进入 24 小时双机运营前，需要补齐平台连接器、中心数据库、对象存储、任务租约和转化归因。

## 2. 营销目标与北极星指标

### 2.1 最终业务目标

```text
优质短视频曝光
  -> JaguarTV Hoje 内容/安装页点击
  -> App 下载
  -> 注册完成
  -> 首次观看
  -> 试用转付费 / 续费 / 分享
```

内容工厂不能以“抓取数量”“生成数量”或“播放量”作为最终成功标准。

### 2.2 指标层级

| 层级 | 指标 | 用途 |
|---|---|---|
| 北极星 | 内容归因的 `first_watch` | 判断内容是否带来真实产品价值 |
| 核心转化 | `registration_completed` | 判断内容是否带来注册用户 |
| 前置转化 | `landing_click`、`download_started` | 定位承接页面和安装路径问题 |
| 内容质量 | 完播率、分享率、收藏率、负反馈率 | 判断内容本身是否有效 |
| 生产效率 | 候选通过率、成片通过率、平均处理时间、失败率 | 判断工厂效率 |
| 虚荣指标 | 播放、点赞、粉丝 | 只用于诊断，不能单独决定扩量 |

### 2.3 每条内容的归因要求

每个 candidate ID 必须对应唯一追踪链接：

```text
https://copa.jarg.top/baixar-o-app
  ?utm_source=tiktok
  &utm_medium=organic_social
  &utm_campaign=hoje_sports
  &utm_content=<candidate-id>_<hook-version>
```

JaguarTV 网站、App 或后台必须将 `campaign_id/candidate_id` 从点击传递到下载、注册和首次观看。缺少这条链路时，内容工厂只能优化播放量，不能优化注册。

## 3. 内容工厂能帮助完成什么

### 3.1 素材发现与库存

- 按关键词发现 YouTube 非葡语候选。
- 保存标题、来源 URL、平台、语言、播放量、评分和状态。
- 通过 candidate ID 去重和交接。
- 从 UI 查看 `DISCOVERED`、`DOWNLOADED`、`READY_FOR_REVIEW` 等状态。
- 直接导入已知 URL，作为搜索不可用时的补充入口。

### 3.2 pt-BR 本地化生产

- 下载源视频并保存来源信息。
- 完全移除源语言音轨。
- 生成巴西葡语脚本、Luciana pt-BR 配音和移动端字幕。
- 加入原创 150 BPM Funk Demo，或从 `assets/bgm/` 自动选择团队曲库。
- 旁白出现时自动降低 BGM 音量。
- 裁掉常见的底部烧录中文字幕区域。
- 输出 9:16、1080×1920、H.264/AAC 成片。
- 添加 JaguarTV Hoje 角标、结尾卡和默认 CTA。

### 3.3 人工审核交付

每条审核目录包括：

```text
workspace/ready_for_review/<candidate-id>/
├── video.mp4
├── cover.jpg
├── metadata.json
└── review.json
```

中间目录保存源视频、脚本、字幕、配音、BGM、QA 和 manifest，便于重做与追踪。

### 3.4 本地控制台

- 总览内容库存和转化漏斗。
- 按状态查看候选和成片。
- 从 UI 启动下载或制作任务。
- 建立 YouTube、Facebook、TikTok、Kwai 发布队列。
- 录入或通过 API 写入平台表现快照。
- 查看关键词候选数量、播放、点击、注册和反馈信号。
- 查看控制节点与未来 Worker 心跳。

### 3.5 团队和 Agent 协作

- WorkBuddy、CodyBuddy、Codex 均可调用 `jaguartv-content-factory`。
- 不依赖 WorkBuddy 运行；独立 CLI 为 `.venv/bin/jaguartv`。
- 侦察、编辑、质检和发布角色通过 candidate ID 交接。
- 共享项目只保留一个任务库，避免成员建立互不一致的副本。

## 4. 当前平台支持矩阵

必须区分“作为素材来源”和“作为发布目标”。同一个平台在两个方向上的支持状态不同。

### 4.1 素材来源支持

| 平台 | 搜索发现 | 指定 URL 下载 | 已实测 | 当前判断 |
|---|---|---|---|---|
| YouTube | 已实现 | 已实现 | 是 | 当前最稳定的 Demo 来源 |
| Bilibili | 已实现适配 | 可尝试 | 搜索遇到 HTTP 412 | 降级来源，需要中国网络 Worker 和稳定适配 |
| 抖音 | 未实现搜索 | yt-dlp 可尝试 | 否 | 需要专用解析器、Cookie/会话和反爬策略 |
| TikTok | 未实现搜索 | yt-dlp 可尝试 | 否 | 需要海外 Worker 和实测适配 |
| Facebook | 未实现搜索 | yt-dlp 可尝试 | 否 | 登录内容需要账号会话或官方入口 |
| Kwai | 未实现搜索 | yt-dlp 可尝试 | 否 | 需要专用适配与巴西网络实测 |
| 快手 | 未实现 | 未验证 | 否 | 需要独立来源适配 |
| Instagram | 未实现 | 未验证 | 否 | 后续作为素材/UGC 导入来源 |

“可尝试”不等于生产支持。只有完成稳定性、登录、限流、失败恢复和批量测试后，才能标记为生产可用。

### 4.2 发布目标支持

| 平台 | 成片/文案生成 | 发布队列 | 自动发布 | 指标自动回传 |
|---|---|---|---|---|
| YouTube | 已生成 | 已支持 | 未接入 | 未接入 |
| Facebook | 已生成 | 已支持 | 未接入 | 未接入 |
| TikTok | 已生成 | 已支持 | 未接入 | 未接入 |
| Kwai | 已生成 | 已支持 | 未接入 | 未接入 |
| Instagram | 可复用竖屏成片 | 未建专用队列 | 未接入 | 未接入 |
| WhatsApp | 可复用视频/文案 | 未接入 CRM | 未接入 | 未接入 |
| JaguarTV Hoje | CTA 已配置 | 不适用 | 未接入站点发布 | 未接入事件回传 |

自动发布需要每个平台真实账号、授权范围、API/连接器和发布审核流程。当前队列只保存计划，不会向外部账号发帖。

## 5. 无法完成或不能承诺的内容

### 5.1 网络和平台限制

- 不能依靠 WorkBuddy 中国沙箱稳定访问 YouTube、TikTok、Facebook 等海外平台。
- 不能保证绕过登录、验证码、设备验证、地区限制或平台反爬。
- 不能保证平台页面结构变化后下载器永远可用。
- 不能在没有账号授权和连接器的情况下自动发布。

正确做法是让中国来源 Worker 部署在中国网络，让海外来源和发布 Worker 部署在可稳定访问目标平台的网络。

### 5.2 内容理解与编辑质量

- 当前 ASR 默认关闭，样片使用标题和描述生成葡语脚本；不能保证完整理解原视频音频。
- 当前不是语义精彩片段检测，主要截取源视频前段；不能自动找到全片最佳 20 秒。
- 当前字幕清除是底部裁切，不是逐帧 OCR 和 AI 修复；动态字幕或遮挡主体时需要人工调整。
- 当前只有一个 macOS pt-BR TTS 音色，不能覆盖男女、多年龄、主持、新闻、故事等栏目需求。
- 当前生成一个通用竖屏版本，没有为四个平台分别重做开头、时长、封面和 CTA。
- 当前标题翻译和脚本规则仍需 pt-BR 编辑审核，不能替代巴西母语编辑。

### 5.3 增长和业务结果

- 不能保证视频热门、账号增长、下载量或注册量。
- 不能在没有网站/App 事件回传时计算真实内容 CAC。
- 不能只根据播放量判断关键词应该扩量。
- 不能直接将高播放视频视为模型训练样本；高播放不等于高注册或高留存。
- 不能承担 WhatsApp 激活、App onboarding、付费和续费的完整职责，这些属于下游增长系统。

### 5.4 系统和团队能力

- 当前 SQLite 适合单主机，不适合两台电脑直接并发写共享文件。
- 当前没有多用户登录、角色权限、密钥管理和完整审计。
- 当前没有 24 小时调度器、Worker 任务租约、死信队列和自动恢复。
- 当前没有 PostgreSQL、MinIO/S3 或远程上传下载。
- 当前反馈模块只生成结构化建议，没有自动训练或自动改写关键词配置。

## 6. 推荐内容矩阵

当前关键词配置只有足球，无法支撑 JaguarTV Hoje 的长期增长。建议采用原营销方案的五个支柱：

| 内容支柱 | 目标比例 | 用户需求 | 主要 CTA | 适合平台 |
|---|---:|---|---|---|
| 今天看什么 | 30% | 今天/今晚有哪些节目和赛事 | JaguarTV Hoje 内容页 | YouTube、Facebook、WhatsApp |
| 体育与球队 | 25% | 比赛、球队、球星和话题解释 | 赛事页/安装页 | TikTok、Kwai、Shorts、Facebook |
| 娱乐与家庭 | 20% | 电影、电视剧、小说、儿童和家庭时段 | 试用/频道发现 | TikTok、Kwai、Instagram、Facebook |
| 巴西地区与文化 | 15% | 本州、本市、地方频道和文化 | 地区页/本地伙伴 | Kwai、Facebook、Instagram |
| 使用与信任 | 10% | 手机、TV、TV Box 安装和官方支持 | 下载/注册/WhatsApp | YouTube、Facebook、WhatsApp |

### 6.1 每周母内容模型

每周至少规划三类母内容：

1. “本周/今天看什么”：连接搜索需求与 JaguarTV Hoje。
2. 体育、娱乐或地区热点解释：负责发现流量。
3. 安装、用户故事或伙伴故事：负责信任与转化。

每条母内容拆为：

- 3–5 条短视频。
- 1 条 Facebook 上下文帖或轮播。
- 1–2 条 WhatsApp Status/Channel 素材。
- 1 个 JaguarTV Hoje 内容页、FAQ 或安装页更新。

## 7. 平台内容运营标准

以下是内部起始标准，需要根据实际数据调整，不应硬编码成永久规则。

| 平台 | 内容任务 | 建议时长 | 版本要求 | CTA 位置 |
|---|---|---:|---|---|
| YouTube Shorts | 搜索、解释、长期视频资产 | 20–45 秒 | 标题回答问题；字幕清晰；可延伸长视频 | 描述、置顶评论、结尾卡 |
| TikTok | 快速发现、强情绪和真人感 | 12–35 秒 | 前 1–2 秒必须独立重做；一个视频一个观点 | 简短口播、简介链接 |
| Kwai | 本地生活、家庭和下沉城市 | 20–45 秒 | 语气自然、真实场景、弱广告感 | 结尾卡、账号简介 |
| Facebook | 30+ 人群、群组和本地商业 | 20–60 秒 | 正文补充上下文；评论有人承接 | 正文链接、评论、WhatsApp |
| Instagram Reels | 品牌、视觉和再营销 | 15–35 秒 | 封面一致、视觉更精致、Stories 配合 | Bio、Stories、评论 |
| WhatsApp | 激活、提醒和召回 | 10–30 秒 | 文件小、信息直接、按分群发送 | 可点击短链接 |

同一成片不能只改文案后全平台发布。至少改变开头、标题、字幕密度、封面和 CTA 中的两项。

## 8. 标准使用流程

### 8.1 首次安装

```bash
cd "/Users/allen/Documents/视频二创"
./scripts/bootstrap.sh
./scripts/install-agent-skill.sh
export JAGUARTV_FACTORY_ROOT="/Users/allen/Documents/视频二创"
.venv/bin/jaguartv doctor
```

### 8.2 启动控制台

```bash
.venv/bin/jaguartv ui --host 127.0.0.1 --port 8787
```

可信局域网共享：

```bash
.venv/bin/jaguartv ui --host 0.0.0.0 --port 8787
```

`0.0.0.0` 版本当前没有用户鉴权，只能在可信内网或 VPN 中使用，不能直接暴露到公网。

### 8.3 每日生产流程

| 阶段 | 负责人 | 操作 | 输出/放行条件 |
|---|---|---|---|
| 选题规划 | 内容负责人 | 确定支柱、平台、关键词、数量 | 今日 brief |
| 素材发现 | Scout | UI 点击“发现素材”或运行 discover | `DISCOVERED` candidate ID |
| 候选筛选 | 内容负责人 | 看标题、来源、语言、评分和巴西相关度 | 进入下载清单 |
| 下载 | Scout/Worker | 对指定 ID 运行 download | `DOWNLOADED` |
| 本地化制作 | pt-BR Editor | 运行 produce，检查脚本和画面 | `READY_FOR_REVIEW` |
| 成片质检 | Reviewer | 检查语言、原音、BGM、字幕、构图、CTA | APPROVED/REVISION_REQUIRED |
| 入发布队列 | Publisher | 选择平台、账号和时间 | QUEUED/SCHEDULED |
| 平台发布 | Publisher Worker | 当前人工；未来连接器执行 | PUBLISHED/FAILED |
| 数据回传 | Analyst/API | 写入播放、点击、安装、注册 | performance snapshot |
| 策略复盘 | Growth Lead | 看注册和首次观看贡献 | 关键词/格式调整建议 |

### 8.4 CLI 示例

```bash
# 发现
.venv/bin/jaguartv discover --platform youtube --limit 10

# 查看候选
.venv/bin/jaguartv list --status DISCOVERED --limit 50

# 下载和制作
.venv/bin/jaguartv download --candidate <candidate-id>
.venv/bin/jaguartv produce --candidate <candidate-id>

# 刷新旧审核页
.venv/bin/jaguartv review
```

### 8.5 Agent 调用示例

```text
使用 jaguartv-content-factory，发现 10 条非葡语足球或娱乐候选。
只返回 candidate ID、来源、巴西相关度和推荐理由，不开始制作。
```

```text
使用 jaguartv-content-factory，制作 candidate <id>。
移除源音轨，使用 pt-BR 配音和可听见的 Funk BGM，检查源字幕残留，返回审核目录。
```

```text
使用 jaguartv-content-factory，质检 candidate <id>。
检查语言、事实、音量、字幕、主体裁切、品牌和 CTA，输出 APPROVED 或 REVISION_REQUIRED。
```

## 9. 团队协作与职责

| 角色 | 主要责任 | 不应承担 |
|---|---|---|
| Growth Lead | 指标、优先级、关键词策略和周决策 | 逐条剪辑 |
| Source Scout | 来源配置、候选发现、初筛和下载 | 最终批准自己提交的内容 |
| pt-BR Editor | 葡语脚本、字幕、配音、平台版本 | 判断平台数据归因 |
| Reviewer | 语言、音画、品牌、事实和 CTA 验收 | 同时生产并最终批准高风险内容 |
| Publisher | 账号、排期、发布失败和评论交接 | 修改未经批准的商业事实 |
| Analyst | 平台、网站、App 数据和反馈建议 | 只凭播放量改变策略 |
| Developer/Ops | Worker、API、存储、备份和平台连接器 | 代替内容团队判断选题 |

协作规则：

1. 所有交接都使用 candidate ID。
2. 同一 candidate ID 不能被两名编辑同时制作。
3. `ready_for_review/<id>/` 是生产和审核边界。
4. 发布失败、制作失败和审核退回必须保留原因。
5. 当前阶段不在工具中建设复杂权限，正式双机和公网部署前再加入身份认证。

## 10. 两台电脑的推荐运行方式

### 10.1 当前可行方式

- 主机运行 UI、SQLite 和视频目录。
- 第二台电脑通过可信局域网访问 UI，但不要直接并发写共享 SQLite 文件。
- 视频文件可以通过共享存储人工同步，或先由主机统一操作。

### 10.2 正式生产方式

```text
中国来源 Worker ----\
                       -> Content OS API -> PostgreSQL
海外来源/制作 Worker --/                  -> MinIO/S3

发布/监控 Worker <---- 发布队列/API <---- 审核库存
平台指标 + JaguarTV 事件 -------------> Analytics/Feedback
```

正式版必须增加：

- PostgreSQL 中心数据库。
- MinIO 或 S3 兼容视频存储。
- Worker 注册、心跳、领任务、租约和超时回收。
- 定时调度、重试上限和死信队列。
- 密钥引用和平台账号隔离。
- 内网/VPN 鉴权、操作日志和数据库备份。

## 11. 优化空间与优先级

### P0：先完成营销闭环

| 优化项 | 当前问题 | 完成标准 |
|---|---|---|
| 内容追踪链接 | CTA 只有统一地址 | 每条成片生成 candidate ID UTM/deep link |
| JaguarTV 事件回传 | 下载、注册、首次观看为 0 | 网站/App 将事件写入 Content OS |
| 平台发布连接器 | 只有队列 | 至少一个真实平台完成授权、发布、结果回写 |
| 平台指标连接器 | 当前手工/API 录入 | 自动拉取播放、互动、链接点击和失败状态 |
| pt-BR 人工审核 | 当前主要是机器规则 | 每条上线内容有母语审核结论 |

完成 P0 后，系统才能根据注册优化内容，而不是根据播放量猜测。

### P1：提高生产质量与产能

| 优化项 | 建议方案 | 价值 |
|---|---|---|
| 完整 ASR | 缓存 faster-whisper 模型或接入稳定 ASR | 基于原视频事实重写，而不是只读标题 |
| 精彩片段检测 | 转录、镜头切分、音量峰值、人物/动作信号联合评分 | 不再固定截取视频前段 |
| 动态去字幕 | OCR 检测 + 区域跟踪 + inpainting/重构 | 处理移动字幕和非底部字幕 |
| 多音色 | 按体育、娱乐、家庭、教程配置 pt-BR 音色 | 减少模板感，提高完播 |
| BGM 曲库 | 按栏目、平台、情绪和节奏标记曲目 | 自动选择更匹配的音乐 |
| 多平台渲染 | 每个平台单独 hook、时长、封面、字幕和 CTA | 降低批量搬运感，提高分发效果 |
| 去重 | 感知哈希、音频指纹、转录相似度 | 避免相同片段重复生产 |

### P1：提高系统稳定性

| 优化项 | 建议方案 |
|---|---|
| 多源适配 | 中国/海外 Worker 分开部署，适配器独立失败 |
| 任务队列 | PostgreSQL `SKIP LOCKED` 或队列服务 + 任务租约 |
| 视频库存 | MinIO/S3，按 candidate ID 和版本保存 |
| 调度 | 周期发现、定时制作、定时发布、失败重试 |
| 可观测性 | Worker 心跳、错误率、队列积压、单条成本和告警 |
| 安全 | API token、账号密钥加密、最小权限和审计 |

### P2：AI 策略反馈

先建立结构化标签，再训练排序模型：

- 题材、关键词、来源账号和源语言。
- 视频时长、前两秒形式、人物数量和镜头速度。
- 配音、字幕密度、BGM、CTA 和平台版本。
- 完播率、分享率、点击率、安装率、注册率和首次观看率。

反馈动作建议分三层：

1. `BOOST_KEYWORD`：增加高注册关键词候选配额。
2. `PREFER_SOURCE`：提高持续产生高质量内容的来源权重。
3. `PREFER_FORMAT`：复用高转化 hook、时长、叙事和 CTA。

只有建议经过人工批准后才修改配置。数据量足够后再训练排序模型；不建议一开始就做 LLM 微调。

## 12. 标准优化流程

每轮只改变一个主要变量，并记录版本。

### 12.1 选题优化

```text
关键词/来源
 -> 候选通过率
 -> 成片发布率
 -> 每千播放点击
 -> 每千播放注册
 -> 首次观看率
 -> 调整关键词和来源权重
```

淘汰“播放高但点击和注册长期为零”的主题；优先扩展“播放一般但注册率和首次观看率高”的主题。

### 12.2 创意优化

为同一主题测试：

- Hook A/B。
- 20 秒与 35 秒。
- 信息型旁白与情绪型旁白。
- Funk BGM 与轻音乐。
- 软 CTA 与明确安装 CTA。

每次只改变一项，并使用不同 `hook_version` 和 `utm_content`。

### 12.3 发布优化

按平台和账号分别观察：

- 发布时间。
- 每日发布频率。
- 相同主题间隔。
- 首小时和 24 小时播放速度。
- 负反馈与账号健康。
- 点击、注册和首次观看贡献。

不将一个平台的胜出时间或视频长度直接复制到另一个平台。

### 12.4 承接优化

内容表现好但注册低时，不应继续扩大爬取量。依次检查：

1. CTA 是否清晰。
2. 链接是否可点击、是否带 candidate ID。
3. 落地页加载速度和 pt-BR 表达。
4. 手机/电视安装路径是否清楚。
5. 下载是否成功。
6. 注册是否过长或跨域丢失归因。
7. 首次打开后是否立即看到值得观看的内容。

## 13. 运营节奏

### 每日

- 查看 Worker、来源和失败任务。
- 审核候选和成片库存。
- 发布已批准内容。
- 处理平台告警、评论和链接问题。
- 回传昨日平台与 JaguarTV 指标。

### 每周

- 哪些内容带来注册和首次观看？
- 哪些来源只制造大量低质量候选？
- 哪个平台/账号的发布失败或负反馈上升？
- 本周停止哪个关键词、来源或格式？
- 下周只增加哪一个测试变量？

### 每月

- 内容归因注册和首次观看总量。
- 各平台每千播放点击/注册。
- 各支柱的成片量与转化贡献。
- 生产成本、失败率和平均处理时间。
- 账号健康、下架、投诉和素材质量问题。
- 是否满足扩量条件，还是应该先修复承接。

## 14. 阶段验收标准

### 阶段 A：本地生产 Demo（当前）

- 能发现、下载、制作真实视频。
- 能生成 pt-BR 配音、字幕、BGM 和品牌成片。
- 能在 UI 中看到库存和任务状态。
- 能人工审核和建立发布队列。

### 阶段 B：可运营内容工厂

- 至少 3 个稳定素材来源。
- ASR、精彩片段、多平台版本和质检可用。
- 每天定时发现和制作，失败可重试。
- 发布库存、账号和审核流程稳定。

### 阶段 C：双机自动分发

- PostgreSQL、对象存储和 Worker 租约上线。
- 至少 2 个平台完成真实自动发布。
- 平台指标自动回传。
- 中国和海外 Worker 分别稳定运行。

### 阶段 D：增长闭环

- candidate ID 串联点击、下载、注册和首次观看。
- 看板可按平台、关键词、来源、格式和账号计算转化。
- 关键词与来源调整依据注册和首次观看。
- AI 反馈建议有审核、版本和回滚机制。

## 15. 管理层需要确认的开放数据

以下数据会直接影响内容优先级和自动化投资：

1. JaguarTV 当前月活、注册、首次观看和付费定义。
2. 下载到注册、注册到首次观看、试用到付费的真实基线。
3. 1、3、6、12 个月留存和续费率。
4. 每个注册用户和首次观看用户允许承担的内容成本/CAC。
5. 巴西本地团队人数、每天可审核数量和发布时间段。
6. 各平台账号数量、账号地区、是否具备 API/Business 授权。
7. JaguarTV 网站、App、注册和支付系统可提供的事件接口。
8. 可用的 pt-BR 真人/TTS 音色与 BGM 曲库。
9. 第一阶段除足球外优先扩展的两个内容支柱。
10. 预计每天发布多少条，以及各平台账号的安全频率。

## 16. 推荐的下一步顺序

1. 先接 JaguarTV 的 candidate ID、下载、注册、首次观看事件。
2. 选择一个发布平台做完整连接器，建议先从 YouTube 或 Meta 中账号条件最成熟者开始。
3. 打通该平台的发布结果和指标回传。
4. 启用完整 ASR，加入精彩片段检测和平台差异版本。
5. 将 SQLite 迁移到 PostgreSQL，并接入 MinIO/S3。
6. 部署中国来源 Worker、海外来源/发布 Worker。
7. 扩展五个内容支柱和账号运营配置。
8. 累积带注册/首次观看标签的数据后，再上线 AI 排序和关键词自动建议。

这条顺序的核心是：先让一条内容从发现到注册完全可测，再扩展到更多平台、更多账号和更大产量。
